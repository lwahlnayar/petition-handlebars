<section class="contain">
  <h1 class="secondLayoutTitle">Edit Your Profile</h1>
  {{>header}}
  <img src="/pizza_hawaii.jpg" alt="pizza hawaii" width= "400px" class="registerLoginImage">
  {{#if error}}
    <p class="errorMessage">Oops! Something went wrong!</p>
  {{/if}}
  {{#if errorPassword}}
    <p class="errorMessage">Your password is weak! Please make sure to have at least 8 characters, at least 1 number, 1 upper and 1 lower case letter.</p>
  {{/if}}
  {{>editForm}}
</section>
