<section class="contain">
  {{>header}}
  <h1 class="editProfileTitle">- Find below the honorable people who signed up for the good cause -</h1>
  <img src="/pizza_hawaii.jpg" alt="pizza hawaii" width= "400px" class="registerLoginImage">
  <ol class="ordered_list">
  {{#signers}}
  {{#if homepage}}
    <li><a href="https://{{homepage}}">{{first_name}} {{last_name}}</a> {{age}} <a href="/list_signups/{{city}}">{{city}}</a></li>
  {{else}}
    <li>{{first_name}} {{last_name}} {{age}} <a href="/list_signups/{{city}}">{{city}}</a></li>
  {{/if}}
  {{/signers}}
  </ol>
  {{{backButton}}}
  <div class="backButton">
    <a href="/petition_home">Go Back</a>
  </div>
  {{>footer}}
</section>
